---
import ServiceOverviewCard from "./ServiceOverviewCard.astro";
---

<style lang="scss">
  @use "../styles/mixins" as *;

  .services-overview {
    @include container(
      "900px",
      var(--padding-m) var(--padding-s) var(--padding-xl)
    );
    display: grid;
    row-gap: 2rem;

    @media screen and (min-width: 767px) {
      @include flex;
      justify-content: space-between;
    }

    h2 {
      text-align: left;
    }

    &--left {
      @include flex("column", "nowrap");
      gap: 3rem;

      @media screen and (min-width: 767px) {
        width: 45%;
      }

      & > div {
        @include flex;
        justify-content: space-between;
        align-items: center;

        a:nth-child(1) {
          color: var(--primary);
          transition: color 0.3s ease;

          &:hover {
            color: var(--secondary-125);
          }
        }

        a:nth-child(2) {
          @include button;
        }
      }
    }

    &--right {
      display: grid;
      grid-template-columns: 20px 1fr 20px;
      grid-template-rows: 20px 1fr 20px;

      @media screen and (min-width: 767px) {
        grid-template-columns: 30px 1fr 30px;
        grid-template-rows: 30px 1fr 30px;
      }

      ul {
        grid-column: 1 / 3;
        grid-row: 2 / -1;
        @include flex("column", "nowrap");
        gap: 3rem;
        padding: var(--padding-m);
        box-shadow:
          inset 0px 0px 0px 1px rgba(173, 173, 173, 0.301),
          2px 0px 5px 0px rgba(57, 65, 75, 0.25);
        background-color: var(--white);

        @media screen and (min-width: 480px) {
          display: grid;
          grid-template-columns: repeat(2, 1fr);
        }

        @media screen and (min-width: 767px) {
          @include flex("column", "nowrap");
          padding: var(--padding-xl);
        }
      }

      span {
        grid-column: 2 / -1;
        grid-row: 1 / 3;
        z-index: -1;
        display: block;
        width: 100%;
        height: 100%;
        background-color: var(--secondary-50);
      }
    }
  }
</style>

<section class="services-overview">
  <div class="services-overview--left">
    <h2>Mes services</h2>
    <p>Découvrez un aperçu de mes <strong>compétences polyvalentes</strong>.</p>
    <p>
      Bien que <strong>spécialisé</strong> dans la <strong
        >pose de cuisines</strong
      >, je propose également une gamme d'autres services.
    </p>
    <div>
      <a href="/">En savoir plus</a>
      <a href="/">Me contacter</a>
    </div>
  </div>
  <div class="services-overview--right">
    <ul>
      <ServiceOverviewCard
        iconName="fa6-solid--faucet-drip"
        serviceName="Plomberie"
      />
      <ServiceOverviewCard
        iconName="healthicons--electricity"
        serviceName="Électricité"
      />
      <ServiceOverviewCard
        iconName="mdi--bricks"
        serviceName="Remplacement de façade"
      />
      <ServiceOverviewCard
        iconName="fa6-solid--screwdriver-wrench"
        serviceName="Entretien & Dépannage"
      />
    </ul>
    <span></span>
  </div>
</section>
